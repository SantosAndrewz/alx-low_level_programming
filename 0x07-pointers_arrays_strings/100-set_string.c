#include "main.h"
/**
 * set_string - sets the value of a pointer to a char.
 * @s: a double pointer to a char
 * @to: a pointer to a char to that s should point to.
 *
 * Return: void
 */
void set_string(char **s, char *to)
{
*s = to;
}
